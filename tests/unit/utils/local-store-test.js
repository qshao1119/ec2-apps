import localStore from '../../../utils/local-store';
import { module, test } from 'qunit';

module('Unit | Utility | local store');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = localStore();
  assert.ok(result);
});
